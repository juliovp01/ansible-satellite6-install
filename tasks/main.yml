---

- include_tasks: set_facts.yml

- include_tasks: registration.yml

- include_tasks: network.yml
  when:
    - "{{ satellite_deployment_set_network }}"

- include_tasks: firewall.yml

- include_tasks: install_software.yml

- include_tasks: install_satellite.yml

- include_tasks: configure_satellite.yml

- include_tasks: configure_capsule.yml
  when:
    - "{{ satellite_configure_capsule }}"
